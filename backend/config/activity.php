<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Activity subject resources
    |--------------------------------------------------------------------------
    |
    | Allow-list mapping of URL resource segments (e.g. "artists") to model
    | classes, used by the per-entity activity history endpoint. Unknown
    | segments return 404. F1+ registers its domain models here.
    |
    */

    'resources' => [
        // 'artists' => App\Models\Artist::class,
    ],
];
