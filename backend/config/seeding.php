<?php

return [
    /*
    | Password given to the team accounts TeamUserSeeder creates. Leave it unset and a random one is
    | generated per account and shown once, which is the safe choice anywhere but a local machine.
    */
    'team_password' => env('SEED_TEAM_PASSWORD'),
];
