# Decisions

Locked decisions for the Bidayaat monorepo. Amendments are appended, never silently edited.

## F0 baseline (from spec)

| # | Decision |
|---|---|
| D1 | Monorepo: `backend/` (Laravel, API only) and `frontend/` (Vue SPA) |
| D2 | ~~Laravel 11+, PHP 8.3, PostgreSQL 16 (Debian image), Redis 7~~ — **amended, see A1** |
| D3 | ~~Local dev with Laravel Sail in `backend/`~~ — **amended, see A1** |
| D4 | Auth: Sanctum SPA cookie auth (stateful), not tokens |
| D5 | Vite proxies `/api` and `/sanctum` to the backend in dev (same-origin) |
| D6 | TypeScript strict mode on the frontend |
| D7 | Locale in the URL: `/ar/...`, `/en/...`. `/` redirects to stored/default locale (`ar`) |
| D8 | API always returns both languages as `{ar, en}`; the frontend picks |
| D9 | Bilingual columns are explicit `_ar`/`_en`, not a translations table |
| D10 | Numerals: Western digits (0-9) in both languages |
| D11 | Fonts: IBM Plex Sans Arabic + IBM Plex Sans, self-hosted via `@fontsource` |
| D12 | Tailwind CSS v4, logical properties only (`ms-`, `me-`, `ps-`, `pe-`, `start-`, `end-`, `text-start`). Forbidden: `ml-`, `mr-`, `pl-`, `pr-`, `left-`, `right-`, `text-left`, `text-right` |
| D13 | Packages installed only when a feature needs them |
| D14 | Every create/update/delete/restore on any tracked model is recorded as an immutable, field-level change record (who, when, model, field, old, new), visible in an admin changelog view. Wired once in F0; every later feature must use it |

**OPEN:** SEO for the public SPA (prerendering vs Inertia SSR vs Nuxt) — decide before F9. F0/F1 keep history-mode routes and stable slugs.

## Amendments

### A1 (F0, 2026-09-19) — Database and dev environment

- **Database: MySQL 8 (via Herd), not PostgreSQL.** Reason: user directive ("use herd and mysql"); Herd already runs MySQL on 127.0.0.1:3306 and Redis on 127.0.0.1:6379, Docker daemon is not running.
- **No Sail / Docker.** Dev services are managed by Herd. `make up` only verifies that MySQL/Redis are listening.
- **PHP 8.4** (Herd's version; satisfies the "PHP 8.3+" floor).
- **CI uses a MySQL 8 service container** instead of Postgres.
- Consequences: F1's `pg_trgm`/GIN trigram search must be re-planned for MySQL (likely a generated fulltext/normalized-column approach) — decision deferred to F1.

## F1 — Artists (2026-09-19)

- **D15 — Search: LIKE on pre-normalized columns, not `pg_trgm`.** (Fulfils the A1 deferral.) `artists.search_text` and `artists.search_compact` are written at save time by `ArtistSearchTextBuilder` from `ArabicNormalizer`-normalized names + name variants; a query is split into tokens and every token must `LIKE '%tok%'` the search_text (AND), OR the whole normalized query matches inside `search_compact` (handles cross-token substrings and missing tatweel). Wildcards are escaped; a punctuation-only query matches everything. Normalization: NFKC, case-fold, Arabic diacritics (U+064B–065F, U+0670) and tatweel stripped, letter folding via explicit map (أإآ→ا, ة→ه, ى→ي, ؤ→و, ئ→ي), Eastern digits → Western, punctuation → spaces. Rationale: MySQL has no pg_trgm; a generated-column fulltext index was rejected because Arabic fulltext tokenization is poor and exact control over normalization was required. Trade-off: tokens are substring matches, so very short tokens can over-match; mitigated by AND-ing tokens.
- **D16 — Statuses are string columns + PHP enums, not MySQL ENUM.** `living_status`, `verified_status`, `publication_status`, variant `type`/`language`, date `calendar`/`certainty` are `VARCHAR` checked by CHECK constraints (raw `DB::statement`, Laravel 13 Blueprint has no `check()`) and backed by `App\Enums\*` string enums. Rationale: adding a value never needs a table rebuild; CHECK keeps the DB honest even outside Eloquent.
- **D17 — Sorting: `COALESCE(name_ar, name_en)` + `id` tiebreaker, whitelisted.** All four list sorts (`name_ar`, `name_en`, plus `-` desc prefixes) order by `orderByRaw('COALESCE(ar, en)')` with a deterministic `id` tiebreaker; anything not in the match whitelist falls back to the default sort. Rationale: either language may be null (D9), and offset pagination requires a stable total order.
- **D18 — Partial dates are five real columns behind one virtual attribute.** `birth_*`/`death_*` column groups (`_date_display`, `_year_from`, `_year_to`, `_calendar`, `_certainty`) are mapped by `PartialDateCast` onto a `PartialDate` value object exposed as a single `birth`/`death` attribute (registered via a `partialDate()` Blueprint macro). Rationale: partial dates must be queryable/sortable later (decades, century filters in F6) without JSON extraction; the VO keeps the API and forms simple.
- **D19 — Two parallel route shapes: `/artists/{slug}` (public GET) vs `/artists/{id}` (writes).** IDs are bound with `whereNumber` on every protected route so a slug can never reach an `{id}` parameter; the public GET-by-slug uses an explicit `[a-z0-9-]+` regex. Consequence: requests like `PATCH /artists/{slug}` 405 (GET-only match) and `POST /artists/{slug}/verify` 404 (no match).
- **D20 — Policy signatures must absorb Gate-stripped class-name arguments.** Laravel's `can:` middleware strips `App\Models\Artist` class-string arguments before calling the policy, so `create`/`restore` are declared as `create(User $user)` / `restore(User $user, ?Artist $artist = null)` while resource-instance checks keep the model parameter.
- **D21 — Rate limiters are disabled under `runningUnitTests()`.** F1's test matrix issues many api-group requests per test; the array-store 120/min limiter persists per process and would flake the suite. Production/local limits are unchanged.
- **D22 — Known limitation: hard-deleted children drop out of parent activity history.** `SubjectActivityController` unions child rows by `subject_id IN (subquery)`; once a row is hard-deleted the subquery no longer returns its id, so only its activity (not the child's identity/subject) remains reachable. Soft-deleted parents are fine (`withTrashed`). Accepted for F1; revisit if a feature needs undeleted history of hard-deleted rows.

## F2 — Holders + Artworks (2026-09-20)

- **D23 — No DB-level CHECK ties `attribution_certainty` to `artist_id`.** MySQL 8 rejects a CHECK constraint on a column that participates in a foreign key's `ON DELETE SET NULL` action (error 3823: "Column 'artist_id' cannot be used in a check constraint ... needed in a foreign key constraint ... referential action"), which `artworks.artist_id` does. The invariant (`unattributed` ⇔ `artist_id IS NULL`) is enforced instead in `ArtworkPayloadRequest::validateAttribution()`. Consequence: a raw SQL write or a hard-delete cascade could in theory leave the two out of sync; accepted since all app writes go through the Form Request and artists are never hard-deleted through normal app flow.
- **D24 — Dimension parsing lives in one reusable class, called from the request layer, not an observer.** `DimensionParser::parse()` is invoked by `ArtworkPayloadRequest::mapDimensions()` when a client sends `dimensions.raw` (or `frame_dimensions.raw`) without also sending structured `height_cm`/`width_cm`/`depth_cm`. If structured values are sent directly, they win and `dimensions_raw` is left untouched (preserves the original source string as an audit trail even after a manual correction). The same class is reused unmodified by `ArtworkSeeder` and is the one F4's importer must reuse.
- **D25 — Holder privacy is enforced in the resource layer via `HolderDisplayResolver`, not by hiding rows.** `is_public_name = false` holders are still stored with their real name and returned from every endpoint, but `HolderResource`/`ArtworkResource` always render the resolved display name (real name, "Private collection, {city}", "Family collection", or "Artist's estate" per `HolderDisplayResolver::resolve()`) and only include the raw `name_ar`/`name_en` and `internal_notes` when the requester has `holders.manage`. There is no public holder list endpoint in F2 (only single-record lookup through an artwork), so private collectors can't be enumerated even in aggregate.
- **D26 — `search_text` on an artwork is resynced when its artist's name changes.** `ArtistObserver::saved()` now also walks `$artist->artworks()` and calls `ArtworkSearchTextBuilder::rebuildQuietly()` on each when `name_ar`/`name_en` changed, so renaming an artist doesn't leave stale search text on their linked artworks.

## F3 — Archive Items, Files, Rights & Access (2026-09-20) — Pass 1 (data model + API)

Pass 1 covers `archive_items`/`files`/`archive_item_links`, access control, policies, the read/write/publish/link API, and seeders. The tus upload endpoint, SHA-256 dedupe, and thumbnail queue jobs are Pass 2, deferred so the riskiest new dependencies (tus-php, Imagick/pdftoppm availability) don't block the core.

- **D27 — OPEN item resolved: `post_embargo_access_level` defaults to `public`, and no intermediate "sees description but not files" tier exists.** Per the spec's own fallback, an embargoed item that has lifted falls through to its explicit `post_embargo_access_level` column (set at write time, defaulting to `public`); `researcher`/`institution` role holders get exactly the tier `ArchiveAccessResolver::userTier()` assigns them (registered/researcher/institution_only) with no separate metadata-vs-files split beyond the existing `canViewMetadataOnly()`/`canViewFull()` boundary. Revisit if a human wants the intermediate tier later.
- **D28 — Access tiers map onto existing F0 roles, no new `registered` role added.** `ArchiveAccessResolver::userTier()`: anonymous → `public`; `verified_researcher` role → `researcher`; `institution` role → `institution_only`; any other authenticated user (`reader`, `contributor`, `artist_claimed`, or `editor`/`admin` without `archive.manage`, though those always short-circuit to full access) → `registered`. There's no dedicated `registered` role in `RolesAndPermissionsSeeder` because "authenticated at all" already is that tier.
- **D29 — No DB-level CHECK ties `access_level = embargoed` to `embargo_until`, for the same MySQL 8/FK reason as D23 — actually this one has no FK conflict, so the CHECK is kept.** (`archive_items_embargo_date_check`.) Noted here only to record that, unlike D23's artist_id case, this constraint could be added safely because `access_level`/`embargo_until` don't participate in any foreign key.
- **D30 — Soft-deleting a parent archive item orphans children instead of cascading.** `archive_items.parent_id` is `nullOnDelete()` at the FK level, but that only fires on a hard `DELETE`, never on Eloquent's `SoftDeletes`. `ArchiveItemObserver::deleting()` explicitly sets `children()->update(['parent_id' => null])` before a soft delete, matching the spec's "deleting a parent sets children's parent_id to null, not cascade-deleting children."
- **D31 — Publish validation reads the persisted record, not the request body.** `POST .../publish` takes only an optional `edit_summary`; `ArchiveItemPublishController::assertPublishable()` validates the archive item's already-saved attributes (type, title/description presence, rights-status-vs-access-level, embargo date) so an editor can't bypass the "no unknown-rights material at public access" rule by omitting a field from the publish call itself.
- **D32 — Archive item links reuse the polymorphic `archive_item_links` table via a real model (`ArchiveItemLink`), not Eloquent's built-in morph-pivot sugar**, because the table carries its own `role` column, timestamps, and needs its own `LogsChanges` audit trail — a plain pivot wouldn't get audited. Search-text resync on link add/remove is wired through `ArchiveItemLinkObserver`, mirroring D26's artist-rename resync.

## F4 — Spreadsheet Importers (2026-09-20)

The F4 spec numbers its own decisions D29–D36; those labels collide with this file's own running numbering (already at D32 from F3), so this section continues the local sequence as D33+ and cross-references the spec's D-numbers in prose instead.

Built in two passes. Pass 1 covers the full pipeline (`import_batches`/`import_batch_rows`/`import_mapping_profiles`, `SpreadsheetParser`, `ColumnMapper`, upload→validate→review→commit API) for the Artist and Artwork importers — the two real spreadsheets named in the spec. Pass 2 adds `HolderImporter` and `ArchiveItemImporter`, reusing the same pipeline unchanged. Still deferred: the frontend admin import UI, and moving large batches off the synchronous request path onto a queue (D33).

- **D33 — Batches always validate inline; the queued-job path (spec's "batches over ~200 rows") is deferred.** `ImportBatchStoreController` calls `ImportValidator::validate()` synchronously in the request. Queuing `ValidateImportBatch`/`CommitImportBatch` jobs for large batches is real work (progress polling, job failure handling) that doesn't change the pipeline's shape, so it's left for when an actual large spreadsheet needs it rather than built speculatively now.
- **D34 — `import_batch_rows.matched_entity_id`/`resulting_entity_id` are `unsignedBigInteger`, not `uuid` as the spec's table literally lists.** Every entity these columns can point to (Artist, Holder, Artwork, ArchiveItem) uses a bigint auto-increment primary key throughout F1–F3; a uuid column couldn't reference them. `import_batches`/`import_batch_rows`/`import_mapping_profiles` themselves still use uuid primary keys as specified.
- **D35 — `activity_log.subject_id`/`causer_id` widened from `unsignedBigInteger` to `VARCHAR(36)`.** F4 is the first feature with `LogsChanges`-tracked, UUID-keyed models (`ImportBatch`, `ImportMappingProfile`); spatie/activitylog's default bigint morph columns can't hold a UUID (`Data truncated for column 'subject_id'`). Widening to a string lets one `activity_log` table serve both bigint- and uuid-keyed subjects. Consequence: `ActivityResource::normalizeId()` casts a purely-numeric string back to `int` so the API response shape is unchanged for existing (bigint-keyed) subjects; the raw `Activity` model's `causer_id`/`subject_id` attributes are now strings even for a `User` causer (a pre-existing test asserting `toBe($user->id)` was updated to `toBe((string) $user->id)`).
- **D36 — `import_batches` gained a `column_map` column beyond the spec's literal table**, holding the resolved map actually used for the batch (copied from the mapping profile, or the ad hoc map submitted at upload). Without it, `/revalidate` would have no way to re-run without the client resending the mapping on every call — the spec's own endpoint description promises revalidation "without re-uploading," which requires the map to be persisted somewhere.
- **D37 — `PartialDate::fromString()` (new) added to the existing F1 value object, not a separate import-only parser.** F1 explicitly deferred free-text date parsing to F2/F4; F2 didn't end up needing it (artwork dates are structured API fields), so this is its first implementation. Mirrors `DimensionParser`'s "never guess, never throw, keep the raw string" philosophy: a plain year, `circa`/`c.`/`نحو` prefixes, an explicit range (`1970-1975`, en-dash or slash variants), and a `hijri`/`H`/`هـ`-suffixed year are recognized; anything else keeps the raw text as `display` with null years and `certainty: unknown`.
- **D38 — Match resolvers are simple, explainable heuristics, not a scoring library.** `ArtistMatchResolver` (spec's D33): exact `legacy_code` first, then a normalized-name `LIKE` lookup — zero hits is `new`, one hit is `matched_suggested` (confidence `high` only if the normalized names are fully equal, else `medium`), more than one hit is `ambiguous`. `ArtworkMatchResolver` (spec's D35): a 4-point score per candidate artwork owned by the same artist (+2 exact normalized title match, +1 each for height/width within 0.5cm), mapped to high/medium/low confidence bands. Both never auto-write — every row still needs an explicit `resolution` before commit.
- **D39 — Holder matching (spec's D34) implements "exact normalized name only, no fuzzy suggestion" by loading and comparing in PHP, not a `LIKE` query.** Unlike Artist/Artwork, `holders` has no `search_text`/`search_compact` column to index against, so `HolderMatchResolver` pulls all holders and compares `ArabicNormalizer::compact()` output directly — acceptable at holder-table scale (dozens to low hundreds of rows), revisit if that stops being true. Zero or one exact-name hit resolves normally; more than one is `ambiguous` (defensive — collisions are unlikely for institution names but not impossible).
- **D40 — Import-only permission `imports.manage`** (→ `editor`, `admin`) gates every `/api/v1/imports*` and `/api/v1/import-mapping-profiles` route as a single blanket permission (like `activity.view`), rather than per-action policies — there's no natural "owner" of an import batch beyond "can this user run imports at all," matching the spec's "editor/admin only, same restriction as F3 publish."
- **D41 — Archive Item matching has no spec'd rule (D33–D35 only cover Artist/Holder/Artwork), so it reuses the simplest applicable pattern: exact `legacy_ref` only, no fuzzy suggestion.** Unlike an artist's name or an artwork's title+dimensions, a scanned article/photo/catalogue has no independent human-recognizable identity to fuzzy-match on; a `legacy_ref` collision is the only safe automatic signal. `ArchiveItemImporter` also creates an `archive_item_links` row (role `subject`) for a resolved `artist_code`, via `firstOrCreate` so re-running an import batch doesn't duplicate the link.
- **D42 — Both `ArtworkImporter` and `ArchiveItemImporter` reject a row when `artist_code` is given but doesn't match any artist**, rather than silently importing it unattributed/unlinked. An editor who typed a real code expects it to resolve; silently dropping the attribution would be a worse failure mode than an explicit, fixable validation error the reviewer sees before commit.

## F10 — Trust & Completeness Dashboard (2026-09-20) — backend

Numbering continues locally (D43+); the F10 spec's own D47–D57 are cross-referenced in prose.

- **D43 — Citable entities are Artist, Artwork, ArchiveItem only.** The spec also lists Event, but F9 (events) isn't built; `config/completeness.php` is the allow-list to extend. `citable_id` columns are `unsignedBigInteger` (all citable entities use bigint ids), same reasoning as D34.
- **D44 — Added `created_by_user_id` to artists/artworks/archive_items.** D52's "my records" dashboard scoping needs a creator, which F1–F3 never recorded. Stamped by observers from `auth()->id()`; existing rows are null ("system").
- **D45 — Rules omit two spec examples with no schema backing.** `portrait_with_clear_rights` (Artist) and `image_usage_permission` (Artwork): there is no artist-portrait or artwork-image/file link yet, so they'd be permanently "missing." Revisit when artwork/artist images exist. A living artist is exempt from a death-year citation (`citationExempt`).
- **D46 — Recompute is inline, not queued** (same simplification as D33). Observers on Artist/Artwork/ArchiveItem and the citation/conflict controllers call `CompletenessCalculator::recompute()` + `DashboardStatsCalculator::recompute()` synchronously.
- **D47 — `record_completeness` has a composite key**, so it is written via `DB::table()->updateOrInsert()`, never Eloquent `save()`; model sets `$table` explicitly (auto-pluralisation gives the wrong name).
- **D48 — Publish gate runs on transition only.** `PublishGate`/`evaluate()` compute live (not from the materialized row) and only when `publication_status` is being *changed to* published on Artist/Artwork **update**, and inside `ArchiveItemPublishController` (merged into its existing itemized 422). Creating an artist/artwork directly as `published` is not gated (no id exists yet to attach citations to; pre-existing F1/F2 behaviour left unchanged). Already-published records are never unpublished (D55).
- **D49 — Review queue is permission-targeted, not role-targeted.** Per-type permissions (`review_queue.archivist_review|data_audit|second_source_needed`) filter `/review-queue`; today editor/admin hold all three since no archivist/auditor roles exist. Mechanism is ready for finer roles. Queue items are only listed/acknowledged; nothing enqueues them yet (F7).
- **D50 — Conflict detection compares exact JSON equality of `claimed_value`** (spec's known limitation on partial-date normalisation stands). Resolution goes through `LogsChanges` on `SourceConflict`.
- **Not yet built:** F10 frontend (DashboardPage, CompletionSidebar, ConflictResolutionModal, per-field Sources panel), Event rules, a `/sources` listing endpoint.

## F11 — Artwork Curation & Acquisition Pipeline (2026-09-20) — backend

- **D51 — F7 stand-in for contributor notes (spec D90).** F7 doesn't exist, so a contributor PATCHing `status_research` with only a `note` gets `202` and a `pipeline_note_suggestions` row; an editor accepts it via `POST /pipeline-note-suggestions/{id}/accept`. Any other contributor stage write is 403. Replace with real F7 proposals later.
- **D52 — Merge re-points only what exists.** `archive_item_links` (dropping ones that would collide on the unique key) and `field_citations`; F7 `edit_proposals`/`revisions` don't exist yet. Tombstone = soft-delete + `artworks.merged_into_id`; `GET /artworks/{old}` answers `301` to the survivor. `field_resolution` maps field → `survivor|duplicate` over a whitelist (`ArtworkMerger::MERGEABLE`).
- **D53 — Approval = all six stages `done|not_applicable` AND no F10 blocking gap** (one itemized 422, keys `data.*` / `pipeline.*`). The spec doesn't say which stages gate; requiring all six is the conservative reading. Approve sets `publication_status = published`.
- **D54 — D89 citation is pipeline-owned.** When `owner_pre_agreement` and `contract_draft` are both cleared, a `Source` (reference_note `pipeline:artwork:{id}`) and an `image_usage_permission` citation are created; reverting either stage deletes them. (F10's Artwork rules still don't require that citation — D45 — so this is currently informational until artwork images exist.)
- **D55 — Stages are lazily ensured** (`PipelineService::ensureStages`) so artworks created before F11 get their six rows on first access.
- **D56 — Candidate detection is out of scope**; only list/promote/dismiss exist. Nothing creates candidates yet (F8 extraction / F4 unmatched-row hook). Promotion links the source archive item to the new artwork with role `depicts`, category defaults to `other`.
- **Not built:** F11 frontend (registry page, curation detail, `MergeToolModal`).

## F12 — Artist Curation & Documentation Pipeline (2026-09-20) — backend

- **D57 — Contact data is encrypted and audit-safe.** `contact_email`/`contact_phone` use Laravel's `encrypted` cast (text columns) and are excluded from `LogsChanges` diffs so plaintext never lands in `activity_log`; `ArtistCurationUpdateController` instead writes an explicit activity entry listing `contact_fields_changed`. Other internal fields (`key_contact_name`, `owner_type`, statuses…) are logged normally. Tests assert none of the internal fields appear in the public artist show/list output or `search_text`. Read-access logging is not implemented (spec leaves it open).
- **D58 — `owner_pre_agreement_status` also accepts `not_applicable`.** The §2 enum omits it but the §3 gate accepts it; the column allows all five values.
- **D59 — Verify gate changes F1 behaviour.** `POST /artists/{id}/verify` with `status=verified` now returns one itemized 422 (`data.*` from F10 + `pipeline.authorization_letter`/`pipeline.owner_pre_agreement`) unless all three conditions clear; `disputed` and unverify are ungated. The existing F1 verify test was updated to satisfy the gate. `name_verified` is computed from a `field_citations` row with `field_key = 'name'`, never stored.
- **D60 — "ظهور عام" is computed, not stored:** `public_visibility` = `visible` iff `verifyErrors()` is empty.
- **D61 — Checklist reports `portrait` as unsupported/unmet** (no artist-portrait schema; same gap as D45). "Priority materials" has no flag in the schema, so the `has_priority_materials` filter means "has any linked archive item".
- **D62 — Artist merge mirrors artwork merge (D52):** re-points artworks, archive links, citations, name variants (dropping duplicate names) and themes; tombstone = soft-delete + `merged_into_id`, and `GET /artists/{oldSlug}` answers `301` to the survivor. F7 proposals not moved (don't exist).
- **D63 — `artist_staff_assignments` (optional in spec) not built;** `ref_supervisor_note` free text only. `bio_source_type` is set manually (no auto-compilation from linked materials).
- **Not built:** F10/F11/F12 frontends.

## F10 — Dashboard frontend (2026-09-20)

- **D64 — `/{locale}/dashboard` is open to any signed-in user** (own records only; the API enforces scoping and `dashboard.manage` for other users). No per-page permission check, unlike the admin pages. Viewing another contributor or the org aggregate has no UI yet (API supports `?user_id=` / `?scope=org`).
- **D65 — Row actions link to public detail pages** (artist by slug, artwork by id) because no admin edit UI exists; archive items have no page yet (F3 frontend), so "Complete/Follow up" renders as inert text for them. The spec's "add new record" button and "scroll to first missing field" are not built for the same reason. The collector now returns `slug` for artists.
- **D66 — Added `success` / `success-soft` color tokens** so D56's green ("clear") exists in the design system.
- **D67 — Filters are single-select** (record type, status) in the URL query, not the API's multi-select arrays. Records are grouped client-side per page. The per-field "Sources" panel on record edit pages is not built (no edit pages).
- Verified by typecheck, lint, Vitest (100 tests, incl. i18n parity) and a production build; **not viewed in a browser** (no browser tool available).
- **D68 — The dashboard is the post-login landing page.** `LoginPage` defaults its redirect to `dashboard` (an explicit `?redirect=` still wins) and the `guestOnly` guard sends signed-in users there instead of home. The public home stays reachable via the logo. Dashboard visuals were then aligned to the mockup: bordered four-tile strip with hints, ruled group/sidebar headings, "view all" per group (sets the type filter), and explanatory note banners built from the record's own gaps/conflicts. Still not in the UI: "add new record", per-row completion bar placement, and queue entries showing record titles/ages (the review-queue API doesn't return them).

## F12 — frontend (2026-09-20)

- **D69 — Artists registry (`/admin/artists`) and curation detail (`/admin/artists/:id`)**, gated by `artists.manage` (in-page check like the other admin pages) with nav link "Artists registry". Filters (search, unverified, city, owner type, theme, has-linked-materials) live in the URL; text inputs debounce.
- **D70 — Merge modal is artist-specific and limited.** F11's `MergeToolModal` was never built (F11 has no frontend), so this one is new. Per-field choices cover only what the curation bundle exposes (`name_ar/en`, `bio_ar/en`, `key_contact_name`, `owner_type`); other `ArtistMerger` fields default to the survivor. Default per D94: keep the survivor, take the duplicate's value where the survivor's is empty.
- **D71 — Not built:** "+ artist" (no create UI), editing name/bio/city/life-dates or portrait upload (identity fields are read-only; only identified-through, contact, pipeline statuses, bio source and supervisor note save), theme assignment UI (themes display and filter only), linking materials to their pages. The Verify button's tooltip is the first `verify_blockers` message from the API, so UI and API never disagree.
- Verified by typecheck, lint, Vitest (109 tests) and build; not viewed in a browser.
- **D72 — Curation page re-laid out to the mockup.** Breadcrumb, type/status/materials badges, "name · city" subtitle, portrait placeholder card, red checklist card with a required-fields bar, read-only "work path" badge list (materials, authorization letter, pre-agreement, name verified, public visibility), identity grid with red highlights for unverified name and missing life dates plus a missing-fields count, bio box with provisional note, contact section (authorization/pre-agreement selects moved here, file name shown) with a "complete" tag, and materials with placeholder thumbnails and years. The curation bundle gained `city`, `life_dates`, `authorization_letter.file_name` and per-material `year`. Skipped: "high-priority materials" row (no priority concept, D61) and the display of thumbnails/portrait images (no image data).
- **D73 — Add artist is a full page, not a modal** (`/admin/artists/new`, linked from the registry's "+ Artist" and the dashboard's "Add new record" for `artists.manage`). It reuses the curation layout: live checklist card, identity, bio, and internal contact/pipeline sections. Save POSTs a draft artist, then PATCHes the internal fields (if that second call fails the record still exists and you land on its page with a notice), then opens the curation page. Only artists can be added so far; the dashboard button needs a record-type choice once artwork/archive create screens exist.

## Artist profile data (education, awards, exhibitions, socials, contacts, portrait)
- Contacts are a child table (`artist_contacts`, encrypted email/phone) replacing the single contact columns; audit entries record counts only, never values.
- Education/award/exhibition entries share `artist_entries`; social links live in `artist_social_links` with `is_public` (default false).
- Portraits are stored on the private disk and streamed via the API; public only when rights are licensed/public_domain and the artist is published. `portrait_with_clear_rights` is now a real completeness check.
- On the curation page birth/death are only sent when edited, so dates the date picker cannot represent (circa, ranges, hijri) are never overwritten.
- Frontend type checking: use `npm run typecheck`; bare `vue-tsc --noEmit` is vacuous in this repo.
- Awards, exhibitions, talks and symposiums share one "activities" group in the API and UI; each entry carries a `type` (`artist_entries.type`, no schema change). Education stays its own group.

## Artworks registry and curation screens
- `GET /artworks/{id}/curation` (artworks.manage) is the single bundle for the detail page: fields, 10-item checklist (same completeness engine plus artist authorization letter), pipeline stages, approve blockers and linked archive items. Saves use the existing `PATCH /artworks/{id}`; stages use the pipeline PATCH; Approve uses the approve endpoint.
- The year is only sent on save when edited, so circa/range creation dates are never overwritten.
- Artist and holder are chosen with a shared search picker (`EntityPicker`); holders are searched through `GET /admin/holders` (artworks.manage), artists through the existing admin artists list. The registry filters by both via the URL.
- Add-artwork creates a draft (no artist means `unattributed`) and opens the detail page; the merge modal reuses `POST /artworks/merge` with per-field survivor/duplicate choice.
- Artwork images live in their own `artwork_images` table (private disk, streamed via the API, rights per image, one `is_final`), because `files` is tied to archive items; the unused `final_selected_hr_image_file_id` column was dropped. An image is public only when its rights are licensed/public_domain and the artwork is published. The card/main image is the final image, else the first upload. Merging moves images to the survivor and clears their final flag.
- Not built: thumbnails/resizing (originals are served), tus/resumable uploads, and F3 archive-item file uploads.
- The add-artwork page uses the same two-column layout as the detail page. Both share `useArtworkForm` and `ArtworkFormSections`; the add page queues images locally and uploads them after the artwork is created (a failed upload leaves the artwork created and links to it instead of navigating).

## Archive registry and edit screens
- `GET /admin/archive-items` (archive.manage) feeds the registry: counts (all / mine / incomplete), filters by type, status (draft/published/hidden/incomplete/under review), rights, period and "mine". "Mine" comes from the audit log's `created` entries, since archive items have no creator column. The row badge precedence is under review, then published, then incomplete, then the stored status.
- Bulk actions (`POST /admin/archive-items/bulk`: set status, link to an artist, delete) report success or failure per item, so items rejected by the publish gate are listed instead of failing the batch. Publishing in bulk needs `archive.publish`.
- New archive item columns: place, people names, keywords, source entity, verification reference. The mockup's 3 access options map to `public`, `registered` (restricted) and `institution_only` (hidden); other stored levels stay selectable.
- One original file per item, on the private disk and streamed through `/archive-items/{id}/files/{file}/download` with the existing access resolver; a new upload replaces the old one. No thumbnails, resumable uploads or derivatives yet.
- The publishing checklist (Arabic title, type and file, exact date, rights holder and license, people names for images) is computed server-side (`ArchiveItemChecklist`); "Send for review" requires all of it and creates a pending `archivist_review` queue item. A plain calendar date is required for "exact", and an existing approximate date is only replaced when the user picks a new one.

## Amendments from real archival material (D105–D111)
- **D105** `artist_contacts.address` (encrypted, internal-only, never in audit diffs). Contacts live in a child table, so the address is per contact rather than a single artist column.
- **D106** `artworks.material_classification` (`movable` default, `immovable`, `digital_native`, `unspecified`); editable in the add/edit forms, returned publicly, mergeable.
- **D107** `artworks.conservation_risk_note`, internal-only (not on the public resource), independent of `condition_report_status`.
- **D108** `ArchiveItemType::documentation_card`; the essay stays as prose in the item's description rather than split into columns.
- **D109** `ArchiveLinkRole::primary_documentation`. The `archive_item_links.role` column was widened from 20 to 30 characters because the value is 21 long (a test caught the truncation).
- **D110** `sources.linked_archive_item_id` (nullable FK). A citation may create a source with only `linked_archive_item_id`; the title is then derived from the item (`Source::displayTitle()`).
- **D111** The stored type of such a source is the marker `archive_item`; `Source::effectiveType()` returns the linked item's own `ArchiveItemType`, so a document is never classified twice.
- **A6–A8 (validation only).** The fuzzy date "أوائل الثمانينيات الميلادية" did not actually parse (no digits → unknown), so `PartialDate::fromString()` now understands Arabic decade phrases: early/mid/late decades become a 4/3/3-year circa span and a bare decade a range. Hijri phrases are left unparsed. The Bugis and Alshalti dates are test fixtures.
- **D112 / A8 follow-up.** The archive edit page no longer demands an exact day. The date control has three precisions (exact date, year only, approximate or range). An approximate date counts for the checklist once `content_date_note` (the reason for the approximation) is filled, the same standard artists and artworks already meet. The checklist key is now `date` instead of `exact_date`. Number inputs bind numbers through `v-model`, so year validation coerces to string first.
- **A9 checks.** An audit of every enum value against its column width found no other tight columns (widest values are 19–20 characters in columns of 20 or more, and `review_type` is 30). A round-trip test now saves and reads back the archive edit fields (place, keywords, source, verification reference, date note, date certainty); contact address and conservation risk already have round-trip tests.

## F9 — Events, timeline and publications (D116–D123)
- **Implemented as specified:** events with `event_type`, bilingual title and description, free-text venue and city, an optional host `Holder`, and `PartialDate` start and end (D116); polymorphic `event_participants` with roles and a per-row note for awards (D117, D121); archive items link to events via `linkable_type = event` and the `event_documentation` role (D118); no publications table (D119); `GET /timeline` as a year-bucketed read path tagged by kind (D120); single-gate `EventCompletenessRules` (D122); one shared `theme_taggables` taxonomy for artists, artworks and events, with `artist_themes` migrated one-to-one and the migration covered by a test that runs it forward and back over real rows (D123).
- **Deviation: ids are integers, not uuids.** Every F10 table (`record_completeness`, `field_citations`, `review_queue_items`, `source_conflicts`) keys its polymorphic record with an unsigned bigint. Making events uuid would have meant widening five tables for no user-visible benefit, so events and participants use the same integer ids as artists, artworks and archive items.
- **New permission** `events.manage` (editor and admin) gates create, edit, participants, themes, publish and the admin registry; reading published events and the timeline is public.
- **Date rule (from D113):** an event's start date counts when it is exact, or approximate with a reason in `date_note`; the reason field and the exact/year/approximate control are shared with archive items (`FuzzyDateField`). The end date is optional and exact only.
- **Completeness:** blocking fields are title (Arabic or English), type, date, venue name and city; host institution, description and at least one participant are important but not blocking.
- **Public visibility:** an event page lists only participants that are themselves published; the events on an artist or artwork page are only published events. Archive items on an event page follow the usual access rules.
- **Not built:** F7 edit proposals (contributor edits are not routed through proposals because F7 does not exist, so only editors can change events), the search index for events (F5), and theme tagging for artworks in the UI (the API supports it).

## F7 — Edit proposals, revisions and rollback (D67–D76)
- **Implemented as specified:** contributor edits become `edit_proposals` rows and never touch the live record (D67); the server recomputes the diff from the live record and ignores any client-sent diff (D68/§3.1); one record per proposal (D69); `review_type` derived from the changed fields (D70); approval applies through the record's ordinary fill-and-save so `LogsChanges`, F10 completeness and the search rebuild all fire (D71); rejection keeps the proposal with its note and changes nothing (D72); every applied change is a `revisions` row, and rollback adds a reversing revision rather than deleting one (D73); drifted fields block approval with a 409 until `confirm_conflict` (D74); rollback is editor/admin only and needs `confirm_unpublish` when it would drop a published record below the publish bar (D75); proposals now populate F10's `review_queue_items` (D76).
- **Added, not in the spec: a per-entity allow-list of proposable columns** (`ProposableFields`). Without it a contributor could propose a change to `publication_status`, `access_level`, `merged_into_id` or `search_text`, and an approving reviewer would apply it through the normal update path without seeing anything unusual. Descriptive and factual columns only.
- **D68 deviation:** proposed citations are stored as payloads on the proposal, not as `pending` rows in `field_citations`. Real rows are created only on approval, which then triggers `ConflictDetector` as §3 step 4 requires. Pending rows in that table would have been visible to F10's completeness before any human reviewed them.
- **Permissions:** the `contributor` role existed with zero permissions, so it now holds `proposals.submit`; `review_queue.editorial_review` was added for the new default review type. Approve, reject and rollback are gated on the target entity's own manage permission.
- **Direct-edit revisions** are recorded by the four entity observers. Creating a record is not a revision of it — revision 1 is its first change. The call must run **before** the search-text rebuild, because `rebuildQuietly()` calls `syncOriginal()`, which wipes the old values the revision needs (a test caught this).
- **"F5 search resync"** is asserted as the `search_text` column being rebuilt; F5 has no separate index in this codebase.
- **Frontend:** one `ProposalsPage` serves both the reviewer queue and a contributor's "my suggestions" (the API scopes by permission), rather than two shells. "Suggest an edit" is a dedicated contributor page for artists and artworks, not a dual-mode admin curation form — contributors cannot reach the curation pages at all, and those pages carry editor-only machinery. Archive items and events have no contributor-facing suggest form yet.
- **`ApiError` now carries the raw response body**, so the 409 conflict and would-unpublish payloads can drive the warning UI.
- **Testing gotcha:** MySQL reorders JSON object keys, so a diff read back from the database cannot be compared with an order-sensitive array assertion.

## F14 — Public material submission and intake (D149–D160)
- **Implemented as specified:** an anonymous, rate-limited `POST /material-submissions` creating a `material_submissions` row plus staged `material_submission_files` (D149–D151, D153); never auto-linked to an artist or artwork, with a regression test that a submitter naming a real artist still ends up unlinked (D152); intake goes into F10's existing `review_queue_items` with the new `material_intake` type (D158); cataloging creates non-public `institution_only` / `draft` archive items, moves the staged bytes into `files`, records `source_material_submission_id`, and links the artist or artwork with the existing `donor` role (D155, D156); reject and withdraw hard-delete the files and keep the row and the reason (D159, D160); the authorization letter status mirrors F12's (D157).
- **Spec bug: `VARCHAR(20)` cannot hold `authorization_pending` (21 characters).** `status` is `VARCHAR(30)`. This is the same trap as `primary_documentation` in D109, and the spec's own suggested width caused it again; a test caught the truncation.
- **File type is judged by content, not the client's claim** (`mimetypes:` runs on the actual bytes). Laravel's testing fake guesses the type from the *filename*, so it can never exercise this; the content tests use genuine `UploadedFile`s. A real PDF is accepted even when named `.jpg` and declared as an image, because the content decides.
- **The 2 GB cap is per submission (sum of sizes) and is enforced after validation.** A single HTTP request is also bounded by PHP's `post_max_size` (500 MB here), so in practice a very large submission cannot arrive in one request; resumable or chunked upload would be a separate piece of work.
- **The attestation is enforced server-side** (`attestation` must be accepted), not just as a client checkbox. The spec calls it an honor-system first line of defense; a submission that skips it is refused.
- **Audit:** `submitter_name` and `submitter_contact` never enter the audit diff (the rule contact data already follows); status changes and staff actions do.
- **Catalog request shape (not in the spec):** `items[]` with `item_type`, a title in either language and the `file_ids` it takes. Every staged file must be assigned to exactly one item, so nothing is left in staging, and a file belonging to another submission is refused. The UI defaults to one item per file with an option to fold them into one.
- **Publishing a submission does not publish its items.** They stay drafts for a person to release; the submission's `published` status means the intake is complete and the letter is signed.
- **Transitions are a map, not free-form:** `submitted → initial_review → cataloging`, cataloging is its own endpoint and lands on `authorization_pending`, and `published` needs a signed or not-applicable letter. Terminal states cannot be reopened.
- **Not built (by design or not in the spec):** submitter-facing status tracking, artist match suggestions, virus scanning, uploading the signed authorization letter itself (the status and a nullable morph column exist, but there is no upload endpoint), and a nav link on the public header; the entry point is the footer's "For owners" link.

## Public artist page redesign
- The public artist page now follows the mockup: a hero (portrait, name, other-language name, place and nationality, biography, theme chips, stats line), a two-column body (career list, archive grid, then the sidebar "About this record" with the correction box). The tabs are gone; artworks, events and "also known as" are kept as sections and in the sidebar, so nothing the old page showed was dropped.
- **Career list:** built from the artist's published, linked archive items, oldest first. Only an *exact* year is stated as a year; anything approximate, ranged or undated reads "Year uncertain", so a vague date is never presented as a fact.
- **Privacy decision, please confirm:** the mockup's sidebar has an "Owner" row ("Artist — private collection, Alahsa") and a "record source" row ("Field visit · date"). Neither is shown. `owner_type` is internal-only under D100 and an existing test enforces that; the source note is free text that can hold names or phone numbers. Only the record *date* is public. To show the owner row, the rule itself has to change (a deliberate decision, with a public-safe derived value such as "private collection" rather than the raw category), and the same goes for a source label. An earlier version of this change exposed `owner_type`; the test caught it and it was removed.
- **Correction box** goes to the F7 suggestion form for a signed-in contributor, to sign-in (returning to that form) for a visitor, and is hidden for a signed-in user who cannot suggest.
- The archive page's "View all" link works because the public archive list now accepts an `artist_id` filter, with a visible "showing one artist" note and a way to clear it.
- Not shown, because nothing supports them yet: archive card thumbnails (no derivatives are generated) and the nav's "Themes" item, which belongs to the separate homepage work.

## Public archive browse page
- The public archive page follows the mockup: title and count ("12 items · sorted by most recently added"), a grid/list toggle, a facet sidebar (material type, place, themes, access) with counts and a Clear link, cards showing type, year (or "n/a"), title, and "artist · place", a "Restricted access" badge, and a Load more button in place of page numbers. The search box and the manager-only "include unpublished" toggle are kept, in the sidebar.
- **Facet counts are disjunctive:** a group's own ticked options are left out of its own counts, so ticking "Article" still shows how many "Image" items there are to add, while every other group narrows to articles. They are computed on the first page of each filter set only (`include_facets=1`); load-more requests skip them. Ticked options that no longer match anything stay listed with a zero so they can always be unticked.
- **Themes now apply to archive items** (`ArchiveItem::themes()` on the shared `theme_taggables`, D123), because the mockup filters archive material by theme and nothing could be tagged before. The archive edit page has a theme checklist, and `PATCH /archive-items/{id}/themes` requires `archive.manage`.
- **"City" is the item's place, and the facet groups by its exact text.** Items have a free-text place (Arabic, else English), not a normalized city, so "الأحساء" and "الأحساء والهفوف" would be two facets. A real city list would need a controlled vocabulary, which nothing has yet; the label is "Place" rather than "City" for that reason.
- **"Access" is derived from the access level:** *Fully available* is `public`, *Preview only* is anything narrower (registered, researcher, institution-only, embargoed). It is a property of the item, not of the viewer.
- **Every card now carries its published artists and place, restricted items included.** The restricted shape still omits the description and the files. Draft or merged artists never appear on a card; a test covers it.
- The URL holds filters and view but never a page, so a shared link always opens at the top; results accumulate in memory and de-duplicate an item that moves between pages.
- Not shown: card thumbnails (no image derivatives exist yet), matching the artist page.

## Header menu
- **Bug:** the visitor menu's "Artworks" and "Archive" were `#works` and `#materials` anchors, and the home page has no such sections (its ids are `themes`, `archive`, `artists`), so both did nothing. "Archive" was also hard-coded as the current page everywhere. "Themes" worked only on the home page, there was no way to reach the artworks page from the visitor menu, and below the `md` breakpoint the menu did not exist at all.
- The menu is now one list of links rendered twice, as the desktop bar and as a mobile panel behind a Menu button (closes on navigation and on Escape), so the two cannot drift apart. Visitors and signed-in users share the same five public links (Artists, Artworks, Archive, Events, Themes); signed-in users get the staff tools their permissions allow after them. A manager's "Archive" is the working registry.
- **Current page** is computed from the URL (the page itself or anything beneath it), so an artist's page keeps "Artists" lit and "Archive" is lit only on the archive. Section links such as "Themes" are never marked current.
- **"Events" points at the Timeline page.** The mockups label the menu item "Events" (الفعاليات), and the timeline is where public events are browsed. The page itself is still titled "Timeline"; rename one or the other if that mismatch bothers you.
- **"Themes" links to the Themes section of the home page** (`/#themes`). There is still no themes page, and the router now scrolls to a hash target from any page. It also stops a query-only change (ticking an archive filter) from jumping the page to the top.

## Methodology page
- A static page at `/about/methodology` ("How this archive is built"), following the mockup: the four stages (collection, digitization, documentation, access), what the four labels mean (verified, under verification, restricted access, approximate year), and the rights and access statement. The footer's "Methodology" link, which was a dead `#` link, now goes to it.
- The two buttons under "Rights and access" (Rights policy, For researchers) are **disabled with a "Coming soon" tooltip**. Those pages do not exist, and writing the policy text is not something to invent; the footer's links of the same names are still dead `#` links for the same reason.
- **One label for an unconfirmed year: "Year uncertain" (سنة غير مؤكدة).** The artist career list and the artworks registry already used it (from their mockups); the methodology mockup said "Approximate year". It is now one shared key, `common.labels.yearUncertain`, which the career list reads directly and the artworks registry flag and the methodology glossary link to, with a test that pins all three to the same string in both languages. The glossary definition was widened to match what the label is applied to: the year is approximate, inferred from the item or its neighbours, **or not stated in the source** (undated items carry the same label).
- The stated behavior ("what has not been verified appears labelled, never hidden") is a promise about how the platform works, not something this page enforces; the record pages are where it is (or is not) kept.
